# Ignore the node_modules directory, as dependencies are installed inside the container
node_modules

# ** THE CRITICAL FIX **
# Ignore the local .env file. This prevents it from being copied into the Docker
# image and overriding the production environment variables from the Railway dashboard.
.env
